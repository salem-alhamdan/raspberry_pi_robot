"""Hardware wrapper modules (LED, RGB LED, button, motors, ultrasonic, ...).

Each module in this package wraps a single piece of hardware with small,
single-purpose functions that are easy to teach one at a time in a
notebook. Pin numbers always come from src/config.py - never hard-code a
GPIO number inside one of these modules.
"""
