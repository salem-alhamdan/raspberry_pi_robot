"""Robot-level behavior modules that compose multiple hardware/ wrappers.

Everything in hardware/ stays independent and reusable on its own; modules
in here (like obstacle_avoidance.py) are where those pieces get combined
into actual robot behaviors.
"""
